/**
 * Billing Dashboard module — public surface (router). The read-only commercial
 * operations console (sales, MRR, events, top plans/addons, payments, errors, Flow
 * status) with filters, charts and CSV export. See docs/admin/BILLING_DASHBOARD.md.
 */
export { BillingDashboardPage } from './presentation/pages';
export { billingDashboardRoutes } from './presentation/routes';

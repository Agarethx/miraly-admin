/** Checkout domain errors — the shared catalog errors under the module's vocabulary. */
export {
  CatalogError as CheckoutError,
  CatalogValidationError as CheckoutValidationError,
  CatalogRepositoryError as CheckoutRepositoryError,
} from '@/shared/catalog';
